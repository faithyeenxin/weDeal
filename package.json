{
  "name": "04_wedeal_final",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "@types/node": "^18.8.5",
    "turbo": "^1.5.5"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "start": "cd apps/server && npm run start",
    "generate": "cd apps/server && npx prisma generate"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=16"
  }
}
